import React from "react";

import { Menu } from "../Menu";

import "./styles.css";

const Header = () => {
	return (
		<>
			<header className="header">
				<Menu />
			</header>
		</>
	);
};

export default Header;
